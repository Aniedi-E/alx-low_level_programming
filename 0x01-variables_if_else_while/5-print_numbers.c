#include <stdio.h>
/**
 * main - containing function
 * Return: 0 wen succesful
 */

int main(void)
{
	int idx;

	for (idx = 0; idx < 10; idx++)
	{
		printf("%d", idx);
	}
	printf("\n");
	return (0);
}
